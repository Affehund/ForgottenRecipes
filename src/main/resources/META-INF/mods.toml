modLoader="javafml" #mandatory
loaderVersion="[38,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
license="GNU GPLv3"
issueTrackerURL="https://github.com/affehund/ForgottenRecipes/issues" #optional
[[mods]] #mandatory
modId="forgottenrecipes" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Forgotten Recipes" #mandatory
displayURL="https://curseforge.com/minecraft/mc-mods/forgottenrecipes" #optional
logoFile="logo.png" #optional
logoBlur=false
authors="Affehund" #optional
description='''Adding recipes that should be in the Caves & Cliffs update'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.forgottenrecipes]] #optional
   # the modid of the dependency
   modId="forge" #mandatory
   # Does this dependency have to exist - if not, ordering below must be specified
   mandatory=true #mandatory
   # The version range of the dependency
   versionRange="[38,)" #mandatory
   # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
   ordering="NONE"
   # Side this dependency is applied on - BOTH, CLIENT or SERVER
   side="BOTH"
# Here's another dependency
[[dependencies.forgottenrecipes]]
   modId="minecraft"
   mandatory=true
   # This version range declares a minimum of the current minecraft version up to but not including the next major version
   versionRange="[1.18,1.19)"
   ordering="NONE"
   side="BOTH"
